import React from 'react'
import { Button } from '@mantine/core'

export default function Login (){
  return (
    <div>
      <Button>
        <a href='/auth/google' />
      </Button>
    </div>
  )
}